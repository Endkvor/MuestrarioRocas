const TablaLateral = (props) => {
    const table = (
        <div className="tabla-lateral-container" >
            <p><span>Tipo:</span><span>{props.tipo}</span></p>
            <p><span></span></p>
        </div>
    )
}